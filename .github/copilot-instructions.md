# Workspace Instructions

- Use Yarn 1 from the repository root.
- Keep frontend packages under `packages/*`.
- Use TypeScript, React, Vite, Vitest, and Oxlint for new frontend packages.
- Prefer functional React components and immutable state updates.
- Run `yarn lint`, `yarn test`, and `yarn build` before completing changes.
